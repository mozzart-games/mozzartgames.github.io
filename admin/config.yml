backend:
  name: git-gateway
  repo: mozzart-games/mozzartgames.github.io
  branch: main
  # To pole mówi CMS-owi, że ma szukać bramki Git Gateway na Netlify, a nie na GitHubie
  site_domain: mozzartstore.netlify.app

publish_mode: editorial_workflow

media_folder: "images"
public_folder: "/images"

collections:
  - name: "blog"
    label: "Blog"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Tytuł", name: "title", widget: "string"}
      - {label: "Data publikacji", name: "date", widget: "datetime"}
      - {label: "Treść", name: "body", widget: "markdown"}
